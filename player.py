class Player:
    
    def __init__(self,label,color):
        # Create a label attribute to store the player sign : X,O
        self.label = label
        # Create a label attribute to store the player color 
        self.color = color
